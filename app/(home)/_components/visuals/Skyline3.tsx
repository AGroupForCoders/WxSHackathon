"use client"

import React from "react";
import { animate, createScope, Scope, eases} from 'animejs';

export default function Skyline3({...props}){
  return(
    <div className=" skyline3 h-[400px] w-full bg-gradient-to-b from-gray-400 to-gray-200/0 " {...props}>
      {/* Placeholder */}
    </div>
  )
}